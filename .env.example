# Copy this file to .env and fill in your values.
# .env is gitignored — never commit real keys.

# Backend endpoint for receipt scanning (AI vision, server-side).
# Set this to your deployed backend URL when the service is ready.
# Without it, the receipt scanning feature shows "not available yet".
EXPO_PUBLIC_RECEIPT_SCAN_URL=
